# reload sxhd config

super + Escape
	pkill -USR1 -x sxhkd

# super + h
#   konsole -e 'less .config/sxhkd/sxhkdrc'

# launcher
alt + d
 	rofi -combi-modi window,run,ssh,drun -show combi -sidebar-mode -cycle

# traslator
super + t
	translator.sh

# Terminal
alt + Return
	alacritty

super + e
	alacritty -t files -e /home/michael/workspace/scripts/vifmrun

super + shift + e
  rofimoji --copy-only --max-recent 10 --skin-tone neutral --prompt TYPE

# super + l
#   betterlockscreen -l dim

# snippets
super + shift + s
	$HOME/workspace/scripts/clipboard-snippets.sh

# clipboard
super + shift + c
  CM_LAUNCHER=rofi CM_OUTPUT_CLIP=0 clipmenu

# Applications
super + shift + m
	alacritty -e neomutt
super + shift + l
	alacritty -e nvim /home/michael/data/buchhaltung/ledger/main.ledger
super + shift + h
	alacritty -e hledger add -f /home/michael/data/buchhaltung/ledger/main.ledger
super + shift + t
	alacritty -e htop
# super + shift + c
# 	rofi -show calc -modi calc -no-show-match -no-sort
# super + shift + s
# 	$HOME/workspace/scripts/rofi-web-search.sh
# super + shift + w
# 	networkmanager_dmenu
super + shift + f
  fd -H -t f . data mjp workspace Downloads .dotfiles .public-dotfiles | rofi -threads 0 -width 50 -dmenu -i -p "Open" | xargs  xdg-open
# super + shift + f
#   fd -H -t f . data mjp workspace Downloads .dotfiles | rofi -modi file-browser -show file-browser -file-browser-stdin -file-browser-follow-symlinks -file-browser-show-hidden
# super + shift + d
#  fd -H -t d . $HOME | rofi -modi file-browser -show file-browser -file-browser-stdin -file-browser-follow-symlinks -file-browser-show-hidden -file-browser-no-desc
